---
phase: 01-secure-infrastructure
task: 0
total_tasks: 0
status: not_started
last_updated: 2026-02-28T19:43:24.197Z
---

<current_state>
Project initialization is COMPLETE. Phase 1 execution has NOT started — no PLAN.md exists yet.
We were beginning manual prerequisite installs on the Mac Mini before planning Phase 1.

The next step is `/gsd:plan-phase 1` to create the detailed plan, then `/gsd:execute-phase 1`.
But first, the operator needs to finish manual installs that require sudo/password.
</current_state>

<completed_work>

## Project Initialization (fully complete, all committed)
- PROJECT.md created — full context from deep questioning + PRD (openclaw-prd.md at /Users/b2/openclaw-prd.md)
- config.json created — YOLO mode, standard depth, parallel execution, quality model profile, all agents enabled
- Research complete — 4 parallel researchers (Stack, Features, Architecture, Pitfalls) + synthesizer → SUMMARY.md
- REQUIREMENTS.md created — 47 v1 requirements across 9 categories (INFR, SECR, MEMR, COMM, TASK, PROP, MODL, PRAG, OCRW)
- ROADMAP.md created — 7 phases, all 47 requirements mapped, success criteria defined
- STATE.md created

## Tool Installs (partially complete)
- Ollama installed and running ✓ (brew services started)
- Trivy installed ✓
- Docker Desktop downloaded and in /Applications ✓ (but NOT launched yet, needs manual config)
- Tailscale NOT installed (needs sudo — `brew install --cask tailscale` in operator's terminal)
</completed_work>

<remaining_work>

## Operator Must Do Manually (requires sudo/password/GUI)
1. Fix Docker Desktop: `sudo mkdir -p /usr/local/cli-plugins` then `brew reinstall --cask docker-desktop`
2. Launch Docker Desktop: `open -a Docker`
3. Configure Docker Desktop GUI: Memory 8GB, CPUs 4, Disk 64GB, Start on login ON
4. Install Tailscale: `brew install --cask tailscale` (needs password)
5. macOS Energy Settings: Prevent sleep ON, Wake for network ON, Auto-restart after power failure ON
6. Get API keys ready: Anthropic ($50/mo cap), Airtable PAT, GHL sub-account, Clay workspace ($100/mo cap)

## Then Claude Can Do
7. Run `/gsd:plan-phase 1` to create detailed Phase 1 plan
8. Run `/gsd:execute-phase 1` to deploy Docker stack, security hardening, HITL, circuit breakers
9. Pull Ollama models: `ollama pull qwen3:8b` and `ollama pull nomic-embed-text` (using 8b not 14b due to 16GB RAM)
</remaining_work>

<decisions_made>

- 16GB RAM constraint: Use qwen3:8b instead of qwen3:14b (PRD spec was for 48GB machine). Docker memory set to 8GB instead of 12-16GB.
- Standard depth (5-8 phases): Research suggested 12 phases, consolidated to 7 for standard depth.
- Quality model profile: Opus for research/roadmap agents. All workflow agents enabled (research, plan-check, verifier).
- YOLO mode with parallel execution.
- Lead pipeline deferred to v2 — user prioritized email triage, proposals, and OCR over lead enrichment.
- Notion chosen for todo/action log (user had no fixed system).
- Full PRD at /Users/b2/openclaw-prd.md (105KB, 11 sections) — contains hardened docker-compose.yml, n8n proxy patterns, HITL tiers, credential management, and 7-phase deployment plan. This is the PRIMARY reference for Phase 1 execution.
</decisions_made>

<blockers>
- Docker Desktop needs manual launch + GUI config (can't be done headless from Claude Code)
- Tailscale install requires sudo password (can't be provided through Claude Code)
- API keys not yet created/gathered by operator
</blockers>

<context>
This is a brownfield project — 175+ research markdown docs already exist in the repo describing OpenClaw architecture. The full PRD (/Users/b2/openclaw-prd.md) has the exact docker-compose.yml, .env template, n8n proxy workflow patterns, HITL config, and step-by-step setup guide. Phase 1 execution should follow PRD Sections 3-4 closely.

Key hardware constraint: Mac Mini has 16GB RAM (not 48GB as PRD assumes). Docker allocations and Ollama model choices need to be adjusted. The PRD's docker-compose.yml resource limits (4GB OpenClaw + 2GB Postgres + 1GB Redis + 2GB Qdrant + 2GB n8n = 11GB) exceed what's safe with 16GB total. Need to reduce limits.

The operator is a solo marketer running OnTrack Marketing (digital marketing for local service businesses). First priority after Phase 1: email triage and Telegram HITL (Phase 3).
</context>

<next_action>
1. Operator finishes manual installs (Docker Desktop launch + config, Tailscale, energy settings, API keys)
2. Run `/gsd:plan-phase 1` to create the detailed execution plan
3. Run `/gsd:execute-phase 1` to deploy everything
</next_action>
